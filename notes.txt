For Exchange Rates, I have decided to go with ECB. There is a markdown file called 'ecb-exchange-rate-api-ingestion.md' in the project root that demonstrates how this works. The file ecb-data.json is a current call to the ECB showing the full data structure. This would indicate the data needs to be stored as EUR as the base, so would need to be converted across currencies when making cross-currency conversions. The system should call the ECB API to get the latest data every 5 mins, and store it in the DB in a dedicated table/s. Then when a query is run that requires conversion, we can reference the latest data.

I think that should be executed / updated via teh crontab worker and pushed to the API or something? Or the crontab worker calls the api endpoint that does this? You make the distinction on how the project is designed - a data job?

As for Flight Providers, i want to hold off that for now, as we don't have access to the Amadeus API, or the Duffel API. However, I have spoken to an insider in the business and found that each TMC may have their own API keys they load into their org profile that can be used when their clients run the calls to get flight search results (or their client org users), then it uses their localised API keys to call the travel provider as prices are different across the board - agreements etc.

Further to that discussion, it was advised sometimes we need to be able to create TMC's as part of a TMC group code. That is, when a TMC is signed up via a vendor, they may all belong to a group code (eg - Flight Centre has multiple locations, and so they want to use a "Group Code" to link the locations). A group code would be generated by the Vendor, and applied to each TMC org that is linked. Thi is similar to a "ChainCode" and "ChainName" which has sub "BranchCodes" for each Branch.

eg:

Flight Centre Travel Group
  - assigned chain name = Flight Centre Travel Group (Australia) Pty Ltd
  - assigned chain code = FCTG_AU (or something useful)

Flight Centre Sydney CBD (location)
  - chain name (assigned): Flight Centre Travel Group (Australia) Pty Ltd
  - chain code (assigned): FCTG_AU
  - assigned branch code: FC_22 (this code does not need to derive from the chain code, sits under the chain code and is linked to a single chain code)

Then, when the Flight Centre Travel Group has a search function enabled for (example) Hotels, it might be a search enabled for Hotels at the specific TMC (Flight Centre Sydney CBD - so only for them) or it might be part of the group access, so every Flight Centre TMC that is a member of FCTG_AU can access the results and the API will merge in the results from that (if they choose to enable it in their TMC profile).

This would require additional search functions to be enabled over time, but they would start with:
- Hotel
- Hotel Chain (separate from Hotel)
- Other
- Tour Operator
- Car Company
- Airline
- Bus Operator
- Cruise
- Railway
- Consolidator
- Insurance
- Charter
- Correspondent
- Hotel Package (separate from Hotel and Hotel Chain)

The vendor (search function) is primarily built to not just support air, but hotel and land-content operations.

This matters because each search type/function that we bring on for each category, will need to have specifics - and merge them into a unified data model for C360 to use (eg - Duffel, Amadeus, other NDC, Low Cost Carriers direct via API << all need to return data that can be structured into a single 'shape'.) For flights, stays, cars, we can see what Duffel has: https://duffel.com/docs/api/overview/welcome

---

Github ticketing

Create a markdown note for me to read, and how to configure the github settings. These would be used by the cinturon360 platform directly. I would like the github ticketing system to allow the following:

- any user logged in can create at ticket, this will use github issues on a private repo to make the ticket, and can attach files
- each ticket will be assigned to a 'queue' (see the issues in the ./legacy-reference folder for more info) and/or create other queues as required
- each issue should be marked with a label for the cat1 and cat2 (cat2 is the sub category of cat1), it should be smart enough to create the labels in github if they don't exist the first time, or give me a shell script to populate them all into the github repo directly, and i will create them myself
- each ticket should use the labels to identify:
  - who raised the issue
  - which org they are a member of
  - what org level they are (eg - Client, TMC, Vendor, GPS Support)
  - the tickets raised for a client should be visible to the dashboard of a TMC
  - the tickets raised for a TMC should be visible to the dashboard of the Vendor
  - the tickets raised for a Vendor should be visible to a dashboard of the GPS Support team (GPS Support will be a special permission that needs to be created for user accounts, and added to their profile via sudo user or another user with this permission only)
  - tickets should be able to be escalated one-level each time if they can't be answered
  - escalated tickets are able to be "sent back" to the level below if notes are added (or not) to the previous level
  - the person who raised a ticket can always see the ticket
  - each escalation / de-escalation should create a "system" note on the ticket history to show it's been escalated or de-escalated
  - if possible, notes on the ticket should be visible (public) and invisible (internal) << if not, can leave that off
  - once a ticket is marked "Closed" it cannot be re-opened unless someone re-opens it via the github issues page directly
  - the text field for tickets should be able to handle text in such a way that github is able to interpret it if markdown and/or HTML editor is used (tinymce - the free version)
- the person who it's "assigned to" should also be managed via a tag, but anyone with the 'Support' role can view tickets that are in the queue of their org and can escalate/de-escalate it
- all tickets should start either within the same org or go to the org above immediately when raised, dependent on what type of issue they are (show me how to change/set that later - should be app controlled)

---

Data jobs

Implement what is useful to start, or ask questions.